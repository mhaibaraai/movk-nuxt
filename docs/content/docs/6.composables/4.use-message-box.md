---
title: useMessageBox
description: 命令式弹出 alert 与 confirm 对话框，以 Promise 形式获取用户的确认结果。
seo:
  title: useMessageBox
  description: Imperatively open alert and confirm dialogs in Movk Nuxt and await the user's decision as a Promise, sharing the MessageBox component's options.
---

## 简介

`useMessageBox` 提供命令式弹窗 API，无需在模板中声明组件即可弹出对话框。它基于 [`MessageBox`](/docs/components/message-box) 组件与 Nuxt UI 的 `useOverlay` 实现。

```ts
const { alert, confirm } = useMessageBox()
```

两个方法的入参均为 `MessageBox` 的 props（`mode` 由方法自动设置）：`title`、`description`、`type`、`icon`、按钮配置等，详见组件文档。

## alert

`alert(options)` 弹出仅含确认动作的提示框，返回 Promise，用户关闭后才 resolve，适合阻塞式提示：

```ts
const { alert } = useMessageBox()

async function onSave() {
  await save()
  await alert({ type: 'success', title: '保存成功' })
  // 用户关闭弹窗后继续
}
```

::component-example
---
name: 'composables-use-message-box-alert-example'
---
::

## confirm

`confirm(options)` 返回 `Promise<boolean>`，把确认/取消映射为布尔值：

```ts
const { confirm } = useMessageBox()

async function onDelete() {
  const ok = await confirm({
    type: 'warning',
    title: '删除确认',
    description: '该操作不可恢复，确定继续？'
  })
  if (!ok) return
  await remove()
}
```

::component-example
---
name: 'composables-use-message-box-confirm-example'
---
::

## 异步确认流程

`confirm` 适合串联异步流程——确认后再执行耗时任务：

```ts
const ok = await confirm({
  type: 'warning',
  title: '提交订单',
  description: '确认后将发起支付',
  confirmButton: { label: '提交' }
})
if (ok) {
  await submitOrder()
}
```

::component-example
---
name: 'composables-use-message-box-async-example'
---
::

::tip{icon="i-lucide-message-square" to="/docs/components/message-box"}
声明式用法与完整 props 见 MessageBox 组件文档。
::
