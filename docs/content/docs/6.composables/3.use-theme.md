---
title: useTheme
description: 读写主题状态（颜色、圆角、字体、图标、深色模式）并导出 CSS 变量或配置代码。
seo:
  title: useTheme
  description: Read and write Movk Nuxt theme state — color, neutral, radius, font, icon set and dark mode — and export the result as CSS variables or app.config code.
---

## 简介

`useTheme` 暴露主题的响应式状态与导出方法，是 [`ThemePicker`](/docs/components/theme-picker) 组件的底层逻辑，可用于自定义主题面板或在业务逻辑中读写主题。

## 返回值

```ts
const {
  // 颜色
  color, primary, primaryColors, neutral, neutralColors,
  blackAsPrimary, setBlackAsPrimary,
  // 圆角 / 字体 / 图标 / 模式
  radius, radiuses, font, fonts, icon, icons, mode, modes,
  // 注入与状态
  style, link, hasCSSChanges, hasConfigChanges,
  // 导出
  exportCSS, exportConfig, resetTheme
} = useTheme()
```

| 分组 | 成员 | 说明 |
| --- | --- | --- |
| 颜色 | `color` / `primary` / `primaryColors` | 主色及可选项 |
| 颜色 | `neutral` / `neutralColors` | 中性色及可选项 |
| 颜色 | `blackAsPrimary` / `setBlackAsPrimary` | 以黑色作为主色的开关 |
| 圆角 | `radius` / `radiuses` | 当前圆角及可选档位 |
| 字体 | `font` / `fonts` | 当前字体及可选项 |
| 图标 | `icon` / `icons` | 当前图标集及可选项 |
| 模式 | `mode` / `modes` | 深色模式（`light`/`dark`/`system`）及可选项 |
| 注入 | `style` / `link` | 需注入到页面的 CSS 变量与字体链接 |
| 状态 | `hasCSSChanges` / `hasConfigChanges` | 是否有相对默认的改动 |

## 读写主题

各状态均为可写的 ref，赋值即生效：

```ts
const { color, mode, radius } = useTheme()

color.value = 'green'
mode.value = 'dark'
radius.value = 0.5
```

[`MThemePicker`](/docs/components/theme-picker) 是上述能力的可视化封装；调整任意维度，页面样式即时跟随更新：

::component-example
---
name: 'composables-use-theme-picker-example'
---
::

## 导出 CSS 变量

`exportCSS()` 将当前主题相对默认值的差异序列化为可写入 `main.css` 的片段，把运行时调试结果固化为默认主题：

::component-example
---
name: 'composables-use-theme-export-css-example'
---
::

## 导出 App Config

`exportConfig()` 生成可直接写入 `app.config.ts` 的主题配置片段：

::component-example
---
name: 'composables-use-theme-export-config-example'
---
::

## 变更标记与重置

`hasCSSChanges` / `hasConfigChanges` 标记主题是否偏离默认值；`resetTheme()` 一键恢复：

::component-example
---
name: 'composables-use-theme-changes-reset-example'
---
::

::tip{icon="i-lucide-box" to="/docs/components/theme-picker"}
开箱即用的可视化主题面板见 ThemePicker 组件。
::
