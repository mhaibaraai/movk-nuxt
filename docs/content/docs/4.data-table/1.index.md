---
title: 介绍
description: 基于 TanStack Table 的声明式数据表格，支持列配置、选择、展开、树形、分页与加载更多。
seo:
  title: DataTable
  description: A declarative, type-safe DataTable for Nuxt built on TanStack Table — column config, selection, expansion, tree data, pagination and load-more, with a single columns array.
---

## 简介

`MDataTable` 是基于 [`@tanstack/vue-table`](https://tanstack.com/table) 与 Nuxt UI `UTable` 封装的声明式数据表格。它把列定义、排序、列固定、列宽拖拽、选择、展开、树形、分页与加载更多统一收敛到一个 `columns` 数组与少量 props，并提供完整的 TypeScript 类型推断。

核心理念：

- **声明式列** —— 用对象数组描述列，数据列、选择列、索引列、展开列、操作列、分组列等通过 `accessorKey` 或 `type` 区分。
- **全局 + 列级开关** —— `sortable`、`pinable`、`resizable`、`truncate`、`tooltip` 等既可全局启用，也可按列覆盖或传函数动态决定。
- **类型安全** —— 列、单元格回调、事件处理均由 `DataTableColumn<T>`、`DataTableProps<T>` 等类型驱动。

## 基础用法

传入 `columns` 与 `data` 即可渲染表格。数据列用 `accessorKey` 绑定字段，`header` 设置列头，`size`/`align` 控制宽度与对齐，`cell` 自定义单元格渲染：

::component-example
---
name: 'data-table-basic-example'
---
::

## 行标识

`rowKey` 指定行唯一标识字段，自动派生 TanStack 的 `getRowId`，是选择、展开、树形等功能正确追踪行的基础：

```vue
<MDataTable :columns="columns" :data="data" row-key="id" />
```

## 加载状态

`loading` 透传给底层 `UTable` 显示加载态：

::component-example
---
name: 'data-table-loading-example'
---
::

## 空值占位

`emptyCell` 设置全局空值占位符（默认 `'-'`），列级 `emptyCell` 可覆盖；传 `false` 关闭占位：

::component-example
---
name: 'data-table-empty-cell-example'
---
::

## 下一步

- [列配置](/docs/data-table/columns) —— 排序、列固定、列宽拖拽、截断与可见性
- [特殊列](/docs/data-table/special-columns) —— 选择、索引、展开、操作列
- [树形数据](/docs/data-table/tree) —— 子行、级联选择与缩进
- [行为与交互](/docs/data-table/rows) —— 选择、展开、点击、右键菜单
- [外观](/docs/data-table/appearance) —— 斑马纹、边框、密度、粘性表头
- [分页](/docs/data-table/pagination) 与 [加载更多](/docs/data-table/load-more)
- [API](/docs/data-table/api) —— 完整 Props / Emits / Slots / Expose
