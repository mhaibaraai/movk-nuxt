---
title: 列配置
description: 数据列的固定、排序、列固定切换、列宽拖拽、文本截断、Tooltip、可见性与函数式配置。
seo:
  title: Columns
  description: Configure DataTable data columns — fixed columns, sorting, column pinning, resizing, text truncation, tooltips, visibility and functional (per-column) configuration.
---

## `fixed` 固定列

`column.fixed` 把列贴到表格两侧，横向滚动时保持可见：

::component-example
---
name: DataTableFixedExample
---
::

## `children` 分组表头

通过 `children` 定义分组列（`DataTableGroupColumn`），形成多级表头：

::component-example
---
name: DataTableGroupExample
---
::

## `sortable` 列排序

全局 `sortable` 启用所有数据列点击表头排序；列级 `sortable` 反向覆盖单列（如 `sortable: false` 锁定不可排序）：

::component-example
---
name: DataTableSortableExample
options:
  - name: sortable
    label: sortable
    items:
      - true
      - false
    default: true
---
::

## `pinable` 列固定切换

全局 `pinable` 让用户点表头图钉在 `left`、`right`、`none` 间循环；列级 `pinable` 优先级更高，可锁住强制位：

::component-example
---
name: DataTablePinableExample
options:
  - name: pinable
    label: pinable
    items:
      - true
      - false
    default: true
---
::

## `resizable` 列宽拖拽

全局 `resizable` 给所有数据列加拖拽手柄；列级 `resizable: false` 锁定单列宽度。`columnResizeMode` 控制 `onChange`（拖动中实时重排）或 `onEnd`（释放后更新）：

::component-example
---
name: DataTableResizableExample
options:
  - name: resizable
    label: resizable
    items:
      - true
      - false
    default: true
  - name: column-resize-mode
    label: columnResizeMode
    items:
      - onChange
      - onEnd
    default: onChange
---
::

## `size` / `minSize` / `maxSize` 列宽

`size` 设定固定列宽（数值或预设 `xs`–`xl`）。不设 `size` 而仅给 `minSize` / `maxSize` 时，列宽由内容自适应：`minSize` 是自适应与拖拽的下限；`maxSize` 是可拖拽到的最大宽度。已设 `size` 的列会被 `minSize` / `maxSize` 双向收敛。

```ts
const columns: DataTableColumn<Person>[] = [
  { accessorKey: 'name', header: '姓名', minSize: 120 },    // 自适应，不小于 120
  { accessorKey: 'bio', header: '简介', maxSize: 240 },     // 自适应，拖拽不超过 240
  { accessorKey: 'id', header: '工号', size: 'sm' }         // 固定预设宽 120
]
```

::note
表格采用 `table-layout: auto` 并撑满容器，自适应列的内容宽度可能超过 `maxSize`（auto 布局忽略单元格 `max-width`）。因此对自适应列，`maxSize` 仅约束拖拽上限；要严格固定列宽请改用 `size`。
::

::note
固定列（`fixed`）需要确定宽度参与 sticky 偏移计算，因此默认固定为 `size`。仅当某一侧只有一个固定列（贴近滚动区的内侧固定列）时，该列才支持按内容自适应——此时设 `minSize` / `maxSize` 即可让它随内容撑开（典型如右侧唯一的操作列）。
::

## 文本截断与 Tooltip

`truncate` 控制纯截断（`true` 单行 / `number` 多行 / `false` 禁用 / 函数动态）；`tooltip` 自带相同行数的截断，并在溢出时浮出完整内容。两者不需同时配置：

::component-example
---
name: DataTableTruncateExample
options:
  - name: truncate
    label: truncate
    items:
      - true
      - false
    default: true
  - name: tooltip
    label: tooltip
    items:
      - true
      - false
      - 1
      - 2
    default: 1
---
::

## 列可见性

`column.visibility` 设默认显隐；`columnVisibilityKeys`（白名单）与 `columnVisibilityExcludeKeys`（黑名单）互斥，同传时白名单优先：

::component-example
---
name: DataTableVisibilityExample
options:
  - name: column-visibility-keys
    label: columnVisibilityKeys
    items: ['id', 'name', 'department', 'role', 'level', 'email', 'joinedAt', 'salary']
    multiple: true
  - name: column-visibility-exclude-keys
    label: columnVisibilityExcludeKeys
    items: ['id', 'name', 'department', 'role', 'level', 'email', 'joinedAt', 'salary']
    multiple: true
---
::

## 函数式配置

`sortable`、`pinable`、`resizable` 接受 `(col) => boolean`，`truncate`、`tooltip` 接受 `(ctx) => boolean | number`，一次声明替代逐列布尔，并可按字段或单元格上下文动态决定：

::component-example
---
name: DataTableFunctionalExample
---
::

::tip
列级 prop 优先级始终高于全局 prop。推荐用「全局开 + 个别列关」或「全局关 + 个别列开」的对照方式表达差异。
::
