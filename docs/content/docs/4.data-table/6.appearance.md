---
title: 外观
description: 组件样式覆盖、分页样式覆盖、斑马纹与边框、视觉密度、内容自适应、空值占位与粘性表头。
seo:
  title: Appearance
  description: Customize DataTable appearance — ui slot overrides, pagination ui overrides, stripe and borders, density presets, fit-content width, empty-cell variants and sticky header.
---

## 组件样式覆盖

`ui` prop 接收 slot class 映射，与主题深度合并，定制 `root`、`th`、`td`、`tbody` 等局部样式：

::component-example
---
name: DataTableUiOverrideExample
---
::

## 分页样式覆盖

`paginationUi.ui` 接收 Pagination 主题 slots，可重写 `root`、`summary`、`actions`、`selectedCount` 等局部 class；`paginationUi` 还可配置 `show`、`pageSizes`：

::component-example
---
name: DataTablePaginationUiExample
---
::

## 斑马纹与边框

`stripe` 控制偶数行底色；`bordered` 支持布尔与对象形态，对象形态通过 `color`/`width`/`style` 自定义纵向边框：

::component-example
---
name: DataTableStripeBorderedExample
---
::

## 视觉密度

`density` 切换 `compact`、`normal`、`comfortable` 三档单元格内边距预设：

::component-example
---
name: DataTableDensityExample
---
::

## 内容自适应宽度

`fitContent` 让表格宽度由列宽决定，不再撑满父容器，适合工具型紧凑表格：

::component-example
---
name: DataTableFitContentExample
---
::

## 空值占位

`emptyCell` 支持字符串、函数模板（返回 VNode）与 `false`，分别用于自定义文本、自定义节点与完全关闭。列级 `emptyCell` 优先于全局：

::component-example
---
name: DataTableEmptyCellVariantsExample
---
::

## 粘性表头

`sticky` 默认开启，关闭后表头随内容滚动；需配合受限高度的滚动容器（如 `ui.root: 'max-h-[40vh]'`）：

::component-example
---
name: DataTableStickyExample
---
::

::note{to="/docs/data-table/rows"}
`rowClass` 与 `rowStyle` 的动态行样式见「行为与交互」；`truncate`/`tooltip` 的文本截断见「列配置」。
::
