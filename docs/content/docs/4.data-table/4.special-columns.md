---
title: 特殊列
description: 选择列、索引列、行固定列与操作列的配置，含树形选择策略、动态按钮与二次确认。
seo:
  title: Special Columns
  description: Configure DataTable special columns — selection (single/multiple, tree strategies, custom checkbox props), index, row-pinning and actions (inline/overflow, async loading, confirm, dynamic state).
---

## 概述

数据列用 `accessorKey` 绑定字段；特殊列通过 `type` 区分，无需 `accessorKey`：

| `type` | 用途 |
| --- | --- |
| `selection` | 行选择（单选/多选，树形支持级联策略） |
| `index` | 行序号 |
| `expand` | 树形展开（见[树形数据](/docs/data-table/tree)） |
| `row-pinning` | 行固定到顶部/底部 |
| `actions` | 行操作按钮 |

所有特殊列同样支持 `visibility`、`pinable`、`resizable`、`fixed`、`size`、`align`。

## 选择列

### 多选

`type: 'selection'` 自动渲染表头与单元格复选框，`v-model:row-selection` 同步选中 id 字典：

::component-example
---
name: DataTableSelectionMultipleExample
---
::

### 单选

`mode: 'single'` 关闭多选并显示自定义 `header`，选中态始终至多保留一个：

::component-example
---
name: DataTableSelectionSingleExample
---
::

### `strategy` 树形选择策略

`strategy` 在树形模式（`children-key`）下生效，控制父子勾选关系：`cascade` 父子级联、`isolated` 独立勾选、`leaf` 仅叶子可选（父节点展示为派生态）：

::component-example
---
name: DataTableSelectionStrategyExample
---
::

### `checkboxProps` 函数式配置

`checkboxProps` 接收上下文返回 props：可按 `scope` 区分表头/单元格、按 `isLeafAggregate` 区分父行派生态、按行数据派生 `disabled`：

::component-example
---
name: DataTableSelectionCheckboxPropsExample
---
::

### rowSelectionKeys 与 treeSelection

`v-model:row-selection-keys` 用数组形态外部受控；组件 expose 的 `treeSelection` 派生 `leaves`、`parents`、`halfSelected`、`strictlyChecked` 等业务视图（通过 `useTemplateRef` 访问）：

::component-example
---
name: DataTableSelectionKeysExample
---
::

## 索引列

`type: 'index'` 默认渲染 `#` 表头，行号随排序、分页自动重新计算，可通过 `header` 自定义：

::component-example
---
name: DataTableIndexExample
---
::

## 行固定列

### `position` 顶部 / 底部固定

`type: 'row-pinning'` 的 `position` 取 `top` 或 `bottom`，把行钉到表头或表尾，`v-model:row-pinning` 暴露 `top`、`bottom` 两组 id：

::component-example
---
name: DataTableRowPinningExample
---
::

### `buttonProps` 动态固定按钮

`buttonProps` 接收 `pinned` 状态返回 `icon`、`color`、`variant`，区分已固定与未固定行：

::component-example
---
name: DataTableRowPinningDynamicExample
---
::

## 操作列

### `maxInline` 内联与折叠

`maxInline` 控制平铺按钮数，超出部分折叠到溢出菜单；`action.divider` 在菜单中插入分隔线：

::component-example
---
name: DataTableActionsInlineExample
---
::

### `actions` 数组与函数式

`actions` 既可传静态数组，也可传 `(ctx) => DataTableAction[]` 按行派生：

::component-example
---
name: DataTableActionsFunctionalExample
---
::

### 异步动作自动 loading

`onClick` 返回 Promise 时按钮自动进入 loading，期间禁用并阻止重复触发（仅作用于内联按钮）：

::component-example
---
name: DataTableActionsAsyncExample
---
::

### `confirm` 二次确认

`confirm: true` 在执行前弹出 MessageBox；`confirmProps` 可动态返回 `title`、`type`、`description`、`confirmText`：

::component-example
---
name: DataTableActionsConfirmExample
---
::

### 动态 disabled 与 visibility

`action.disabled` 与 `action.visibility` 接受布尔或 `(ctx) => boolean`，按行派生禁用或隐藏：

::component-example
---
name: DataTableActionsDynamicStateExample
---
::

## 特殊列的共享字段

`selection`、`index`、`row-pinning`、`actions` 列与数据列一样支持 `visibility`、`pinable`、`resizable`：

::component-example
---
name: DataTableSpecialSharedExample
---
::
