---
title: Agent Skills
description: 通过 Claude Agent Skills 让 AI 编码代理按功能域正确生成 Movk Nuxt 代码。
seo:
  title: Agent Skills
  description: Movk Nuxt ships Claude Agent Skills (SKILL.md) per domain — AutoForm, API, DataTable, components and composables — so AI coding agents generate correct, idiomatic Movk Nuxt code alongside the MCP server.
---

## 什么是 Agent Skills？

Agent Skills 是按 [Agent Skills 规范](https://agentskills.io/specification) 编写的 `SKILL.md` 技能包。与 [MCP Server](/docs/getting-started/ai/mcp)（提供实时文档检索）互补，Skills 直接向 AI 编码代理注入「在何种场景使用 Movk Nuxt 的哪个能力、如何正确书写」的指引，让生成的代码符合本库约定。

Movk Nuxt 提供单一 `movk-nuxt` 技能：入口 `SKILL.md` 承载跨域铁律、MCP 工作流与路由表，按功能域拆分的指引位于 `references/`，由代理按需加载：

| 域 | 适用场景 |
| --- | --- |
| `auto-form` | 用 Zod Schema 驱动生成表单、字段控件、布局与校验 |
| `api` | 数据请求、多端点、认证、业务码、Toast、上传/下载进度 |
| `data-table` | 列配置、树形数据、选择/展开、分页与加载更多 |
| `components` | 独立 UI 组件（DatePicker、SearchForm、StarRating 等） |
| `composables` | useDateFormatter、useTheme、useMessageBox 等工具函数 |

## 安装

技能位于仓库的 [`docs/skills/movk-nuxt`](https://github.com/mhaibaraai/movk-nuxt/tree/main/docs/skills/movk-nuxt) 目录。将整个技能目录复制到项目或个人技能目录即可：

```bash
# 项目级（随仓库共享）
mkdir -p .claude/skills
cp -r docs/skills/movk-nuxt .claude/skills/

# 个人级（仅本机）
cp -r docs/skills/movk-nuxt ~/.claude/skills/
```

::tip{icon="i-lucide-plug" to="/docs/getting-started/ai/mcp"}
建议同时配置 MCP Server，让代理在使用技能时按需检索最新文档与示例。
::
