---
title: useDownloadWithProgress
description: 带进度监控的文件下载 composable，支持 JSON 业务校验、POST 导出、取消、不确定进度。
---

## Usage

`useDownloadWithProgress` 基于原生 `fetch` + `ReadableStream` 实现，复用 `@movk/nuxt` 的端点、认证、业务校验、Toast、`movk:api:*` hook 体系。

后端返回 `application/json` 时走业务校验，**不会**把错误响应当作二进制写入文件；返回二进制时按 `content-length` 计算进度，缺失时 `progress` 为 `null`，UI 据此切 indeterminate。

```vue
<script setup lang="ts">
const { progress, status, error, download, abort } = useDownloadWithProgress()

async function handleDownload() {
  const { aborted, error: err } = await download('/api/export/report', {
    filename: 'monthly-report.pdf',
    onSuccess: name => console.log('完成', name)
  })
  if (aborted) return
  if (err) console.error(err)
}
</script>

<template>
  <UButton :loading="status === 'pending'" @click="handleDownload">下载</UButton>
  <UProgress
    v-if="status === 'pending'"
    :model-value="progress ?? undefined"
    :max="100"
  />
  <UButton v-if="status === 'pending'" @click="abort">取消</UButton>
</template>
```

::note
适用于大文件导出、批量下载等场景。对于无需进度的小文件，直接使用 [`$api`](/docs/api/api-plugin) 即可。
::

## 文件名提取

优先级：

1. `options.filename`
2. 响应头 `Content-Disposition`（支持 RFC 5987 编码）
3. URL pathname 末段（自动去除 query string）
4. `'download'`

```ts
await download('/api/export?token=abc')
// 自动取得 export 作为 fallback 文件名，query 不会污染
```

## 不确定进度

当响应缺少 `content-length`（如分块传输），`progress` 为 `null`。直接通过 nullish-coalescing 映射到 `UProgress` 的 `:model-value`（接收 `undefined` 时自动 indeterminate）：

```vue
<UProgress :model-value="progress ?? undefined" :max="100" />
```

## JSON 业务错误不当文件下载

如果后端误返回 JSON 错误（如 `{ code: 40301, message: 'no permission' }`）：

- 自动识别 `content-type: application/json`
- 走业务校验链路，触发 `onError` + `movk:api:error` hook
- **不会**调用 `triggerDownload`，用户不会得到一个无效的下载文件

## POST 导出

部分场景需要把筛选条件放在请求体里：

```ts
await download('/api/export', {
  method: 'POST',
  body: { ids: [1, 2, 3], format: 'csv' }
})
// 自动补充 Content-Type: application/json
```

## 取消下载

```ts
const { status, download, abort } = useDownloadWithProgress()

download('/api/export/large.zip')

function handleCancel() {
  if (status.value === 'pending') abort()
}
```

取消后：

- `status` 变为 `'aborted'`，`progress` 重置为 `0`
- `error` 保持 `null`（不视为失败）
- 不触发 `onError`，不弹 Toast
- `download()` 返回 `{ data: null, error: null, aborted: true }`

## 认证

自动从 session 读取 token 注入到 `Authorization` 头；可在 `headers` 中覆盖（用户优先）。

```ts
await download('/api/export/private.pdf', {
  headers: { 'X-Custom': 'value' }
})
```

## API

### useDownloadWithProgress&lt;T&gt;()

`useDownloadWithProgress<T = Blob>(): { progress, status, data, error, download, abort }`{lang="ts-type"}

#### Returns

::field-group
  ::field{name="progress" type="Ref<number | null>"}
  下载进度，0-100；缺 `content-length` 时为 `null`，UI 据此切 indeterminate。
  ::

  ::field{name="status" type="Ref<TransferStatus>"}
  传输状态：`'idle' | 'pending' | 'success' | 'error' | 'aborted'`。
  ::

  ::field{name="data" type="Ref<T | null>"}
  二进制路径下为 `Blob`；JSON 路径下为解包后的业务数据。
  ::

  ::field{name="error" type="Ref<ApiError | Error | null>"}
  错误对象；业务错误为 `ApiError`，HTTP/网络错误为 `Error`。中止时为 `null`。
  ::

  ::field{name="download()" type="(url: string, options?: DownloadWithProgressOptions) => Promise<TransferResult<T>>"}
  执行下载。
    ::collapsible
      **Parameters**

      ::field-group
        ::field{name="url" type="string" required}::
        ::field{name="options" type="DownloadWithProgressOptions"}
          ::collapsible
            ::field-group
              ::field{name="method" type="'GET' | 'POST'"}默认 `'GET'`。::
              ::field{name="body" type="unknown"}非 GET 时序列化为 JSON。::
              ::field{name="filename" type="string"}::
              ::field{name="headers" type="Record<string, string>"}用户优先于自动 auth 头。::
              ::field{name="toast" type="RequestToastOptions | false"}::
              ::field{name="endpoint" type="string"}::
              ::field{name="skipBusinessCheck" type="boolean"}::
              ::field{name="onSuccess" type="(filename: string) => void"}::
              ::field{name="onError" type="(error: ApiError | Error) => void"}::
            ::
          ::
        ::
      ::

      **Returns**

      `Promise<TransferResult<T>>`：`{ data: T | null, error: ApiError | Error | null, aborted: boolean }`。
    ::
  ::

  ::field{name="abort()" type="() => void"}
  中止当前下载，`status` 切到 `'aborted'`。
  ::
::

## Changelog

:commit-changelog{prefix="composables" suffix="ts"}
