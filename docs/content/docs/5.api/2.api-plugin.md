---
title: $api
description: 由 api.factory 插件创建的增强型 $fetch 实例，支持认证注入、数据解包、业务检查和多端点切换。
seo:
  title: $api Plugin
  description: Configure the Movk Nuxt API plugin — endpoints, baseURL, auth, response parsing, business codes, toast and the $api instance with .use(name) switching.
---

## 介绍

`$api` 是通过 Nuxt 插件注入的全局 `$fetch` 实例，内置完整的拦截器链（认证注入、业务状态码检查、数据解包、Toast 提示）。它是所有 API composable（`useApiFetch`、`useLazyApiFetch` 等）的底层引擎。

## 基础用法

```ts
const { $api } = useNuxtApp()

// GET 请求（数据已自动解包）
const users = await $api<User[]>('/users')

// POST 请求
const user = await $api<User>('/users', {
  method: 'POST',
  body: { name: 'test', email: 'test@example.com' }
})

// PUT 请求
const updated = await $api<User>(`/users/${id}`, {
  method: 'PUT',
  body: { name: 'new-name' }
})

// DELETE 请求
await $api(`/users/${id}`, { method: 'DELETE' })
```

::note
`$api` 返回的数据已经过拦截器自动解包（提取 `dataKey` 字段），泛型 `T` 直接对应业务数据类型。
::

## 多端点切换

使用 `$api.use(endpoint)` 切换到指定端点：

```ts
const { $api } = useNuxtApp()

// 使用默认端点
const users = await $api('/users')

// 切换到 admin 端点
const adminUsers = await $api.use('admin')('/users')

// 保存端点引用，避免重复 use()
const adminApi = $api.use('admin')
const config = await adminApi('/config')
const stats = await adminApi('/stats')
```

端点实例内部有缓存机制，多次调用 `$api.use('admin')` 返回同一实例。

### 默认端点

`useApiFetch` 未传 `endpoint` 时走 default 端点的 `baseURL`：

::component-example
---
name: 'api-endpoints-default-example'
---
::

### 切换至别名端点

`endpoint` 选项按别名切换，请求自动改用对应 `baseURL`：

::component-example
---
name: 'api-endpoints-v2-example'
---
::

### 命令式调用

脱离 `useFetch` 上下文，直接通过 `$api.use(name)` 取得端点实例并发起裸请求：

::component-example
---
name: 'api-endpoints-imperative-example'
---
::

### 当前生效配置

`runtimeConfig.public.movkApi` 暴露端点列表与默认端点名，便于运行时调试：

::component-example
---
name: 'api-endpoints-runtime-example'
---
::

::tip{icon="i-lucide-settings" to="/docs/getting-started/configuration#endpoints"}
了解如何配置多个 API 端点
::

## 认证集成

模块自动接入 `nuxt-auth-utils`：登录后从 `session[sessionTokenPath]` 读取 token，所有 `$api` 请求自动注入 `Authorization` 头；遇到 401 时按 `auth.unauthorized` 策略清理 session 或跳转登录页。

### 当前 Session

`useUserSession` 暴露的响应式 session；登录 / 登出后调用 `fetch()` 同步状态：

::component-example
---
name: 'api-auth-session-example'
---
::

### 调用受保护接口

`useApiFetch` 自动携带 Authorization 头访问 `/demo/protected`；未登录时返回 401：

::component-example
---
name: 'api-auth-protected-example'
---
::

### 401 处理策略

`auth.unauthorized` 控制重定向与 session 清理；docs 站点为便于演示关闭重定向：

::component-example
---
name: 'api-auth-unauthorized-example'
---
::

### 令牌注入格式

`headerName` + `tokenType` 决定最终请求头形态；后端 echo `authHeader` 字段供核对：

::component-example
---
name: 'api-auth-token-injection-example'
---
::

## 数据解包与业务校验

`$api` 默认按 `response.dataKey` 抽取业务数据并按 `successCodes` 校验业务码；两个开关 `skipUnwrap` / `skipBusinessCheck` 让调用方按场景接管。

### 默认解包

按默认 `data` 字段抽取业务数据，调用方直接拿到去信封后的对象：

::component-example
---
name: 'api-unwrap-default-example'
---
::

### 拿原始信封

`skipUnwrap: true` 不重写 `response._data`，保留完整 `code/message/data` 信封：

::component-example
---
name: 'api-unwrap-skip-unwrap-example'
---
::

### 跳过业务校验

legacy 接口未返回 `code` 字段，跳过校验后仍按 `dataKey` 解包出业务数据：

::component-example
---
name: 'api-unwrap-skip-business-check-example'
---
::

### 接管外部接口

外部接口信封非标，同时开两开关由调用方完全自处理响应：

::component-example
---
name: 'api-unwrap-combined-example'
---
::

## Toast 提示

`$api` 内置 Toast 提示，按 `toast.successCodes` 判定成功 / 失败；支持全局默认 + 请求级覆盖两层定制。

### 默认行为

未传 `toast` 时继承全局配置，成功 / 错误均按默认样式弹出：

::component-example
---
name: 'api-toast-default-example'
---
::

### 完全静音

`toast: false` 关闭所有单次提示：

::component-example
---
name: 'api-toast-silent-example'
---
::

### 覆盖文案

`successMessage` / `errorMessage` 替换默认文案：

::component-example
---
name: 'api-toast-message-override-example'
---
::

### 仅静默成功

`{ success: false }` 关闭成功提示但保留错误提示：

::component-example
---
name: 'api-toast-error-only-example'
---
::

### 自定义样式

成功 / 错误分别覆盖 `color`、`icon` 等 ToastProps：

::component-example
---
name: 'api-toast-custom-style-example'
---
::

## 在事件处理器中使用

事件处理器中使用 `$api` 是最常见的场景：

```vue
<script setup lang="ts">
const { $api } = useNuxtApp()

// 表单提交
async function handleCreate(formData: CreateUserRequest) {
  try {
    const user = await $api<User>('/users', {
      method: 'POST',
      body: formData
    })
    // Toast 已自动显示，无需手动处理
    navigateTo(`/users/${user.id}`)
  }
  catch (error) {
    // 业务错误 Toast 已自动显示
    // 此处可做额外逻辑（如表单字段高亮）
  }
}

// 删除确认
async function handleDelete(id: string) {
  await $api(`/users/${id}`, { method: 'DELETE' })
  await refreshNuxtData() // 刷新页面缓存数据
}
</script>
```

## 在 useAsyncData 中使用

当需要聚合多个接口或做复杂数据处理时，在 `useAsyncData` 中使用 `$api`：

```ts
const { $api } = useNuxtApp()

// 聚合多个接口
const { data: dashboard } = await useAsyncData('dashboard', async () => {
  const [users, stats, recent] = await Promise.all([
    $api<User[]>('/users'),
    $api<Stats>('/stats'),
    $api<Activity[]>('/activity/recent')
  ])
  return { users, stats, recent }
})
```

### 封装自定义 composable

```ts [composables/useDashboard.ts]
export function useDashboard() {
  const { $api } = useNuxtApp()

  return useAsyncData('dashboard', async () => {
    const [users, stats] = await Promise.all([
      $api<User[]>('/users'),
      $api<Stats>('/stats')
    ])
    return { users, stats }
  })
}
```

### 业务操作 composable

对于删除、审批等写操作，将 `$api` 调用封装到独立的 composable 中，保持页面文件精简：

```ts [composables/useUserActions.ts]
export function useUserActions() {
  const { $api } = useNuxtApp()

  async function remove(id: string) {
    await $api(`/users/${id}`, { method: 'DELETE' })
    await refreshNuxtData()
  }

  async function create(data: CreateUserRequest) {
    return await $api<User>('/users', {
      method: 'POST',
      body: data,
    })
  }

  async function updateRole(id: string, role: UserRole) {
    return await $api<User>(`/users/${id}/role`, {
      method: 'PUT',
      body: { role },
      context: { toast: { successMessage: '角色已更新' } },
    })
  }

  return { remove, create, updateRole }
}
```

```vue [pages/users/index.vue]
<script setup lang="ts">
// 数据获取用 useApiFetch
const { data: users, refresh } = await useApiFetch<User[]>('/users')

// 写操作用 $api composable
const { remove, updateRole } = useUserActions()
</script>
```

::tip{icon="i-lucide-layers" to="/docs/best-practices/composable-patterns"}
了解完整的 Composable 层设计模式和薄 View 层理念
::

## 请求级配置

通过 `context` 字段传递请求级配置：

```ts
const { $api } = useNuxtApp()

// 禁用 Toast
await $api('/users', {
  context: { toast: false }
})

// 自定义 Toast 消息
await $api('/users', {
  method: 'POST',
  body: formData,
  context: {
    toast: { successMessage: '创建成功' }
  }
})

// 跳过业务状态码检查（直接返回原始解包数据）
const raw = await $api('/external-api/data', {
  context: { skipBusinessCheck: true }
})
```

::note
`context` 字段通过 ofetch 的 `FetchOptions` 扩展传递，仅 Movk Nuxt 的 `$api` 实例识别此字段。原生 `$fetch` 不会处理这些选项。
::

## API

### ApiInstance

`$api` 的类型定义：

```ts
type ApiInstance = $Fetch & {
  use: (endpoint: string) => ApiInstance
}
```

::field-group
  ::field{name="$api(url, options)" type="Promise<T>"}
  发送 API 请求。支持所有 ofetch 的选项，额外支持 `context` 字段。
  ::

  ::field{name="$api.use(endpoint)" type="ApiInstance"}
  切换到指定端点，返回该端点的 `$api` 实例。端点实例内部有缓存，多次调用返回同一实例。
  ::
::

### ApiFetchContext

通过 `options.context` 传递的扩展配置：

::field-group
  ::field{name="toast" type="RequestToastOptions | false"}
  Toast 提示配置。设为 `false` 禁用所有 Toast。
  ::

  ::field{name="skipBusinessCheck" type="boolean"}
  跳过业务状态码检查。设为 `true` 时不抛出 `ApiError`，但仍会解包 `dataKey` 字段。
  ::
::
